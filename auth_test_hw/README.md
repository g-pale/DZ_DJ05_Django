# Auth Test HW - Django проект с системой аутентификации

Проект представляет собой современное веб-приложение на Django с полноценной системой аутентификации и авторизации, использующее кастомную модель пользователя и привлекательный современный дизайн.

## 📋 Описание

Проект реализует систему аутентификации Django с расширенной моделью пользователя и современным пользовательским интерфейсом. Включает в себя:
- Регистрацию новых пользователей с дополнительными полями
- Вход и выход из системы
- Профиль пользователя (доступен только авторизованным)
- Кастомную модель пользователя с полями email и phone_number
- Современный адаптивный дизайн с градиентами и анимациями

## 🛠 Технологии

- **Python 3.x**
- **Django 6.0**
- **SQLite** (база данных по умолчанию)
- **HTML5 / CSS3** (современный дизайн)

## 📁 Структура проекта

```
auth_test_hw/
├── auth_test_hw/          # Основная конфигурация проекта
│   ├── settings.py        # Настройки проекта
│   ├── urls.py           # Главный файл маршрутизации
│   ├── wsgi.py           # WSGI конфигурация
│   └── asgi.py           # ASGI конфигурация
├── main/                  # Основное приложение
│   ├── models.py         # Модель CustomUser
│   ├── views.py          # Представления (views)
│   ├── forms.py          # Формы (CustomUserCreationForm)
│   ├── urls.py           # Маршруты приложения
│   ├── admin.py          # Настройки админ-панели
│   ├── static/           # Статические файлы
│   │   └── css/
│   │       └── style.css # Основные стили
│   └── templates/        # HTML шаблоны
│       ├── base.html     # Базовый шаблон
│       ├── index.html    # Главная страница
│       ├── login.html    # Страница входа
│       ├── register.html # Страница регистрации
│       └── profile.html  # Страница профиля
├── manage.py             # Утилита управления Django
└── db.sqlite3            # База данных SQLite
```

## 🚀 Установка и настройка

### 1. Клонирование репозитория

```bash
cd /Users/sergejsaburkin/PythonProject/DZ_DJ05_Django/auth_test_hw
```

### 2. Создание виртуального окружения (рекомендуется)

```bash
python -m venv venv
source venv/bin/activate  # Для macOS/Linux
# или
venv\Scripts\activate     # Для Windows
```

### 3. Установка зависимостей

```bash
pip install django
```

### 4. Применение миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### 5. Создание суперпользователя (опционально)

```bash
python manage.py createsuperuser
```

### 6. Запуск сервера разработки

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000/`

## 📝 Основные функции

### Модель пользователя (CustomUser)

Кастомная модель пользователя расширяет стандартную модель Django следующими полями:

- **email** (EmailField) - уникальное обязательное поле
- **phone_number** (CharField) - необязательное поле для номера телефона

Модель наследуется от `AbstractUser` и сохраняет все стандартные поля Django (username, password, first_name, last_name и т.д.).

### Страницы приложения

#### 1. **Главная страница** (`/`)
- Доступна всем пользователям
- Современный hero-блок с приветствием
- Карточки с описанием функций приложения
- Кнопки быстрого доступа для неавторизованных пользователей
- Приветствие для авторизованных пользователей

#### 2. **Регистрация** (`/register/`)
- Форма регистрации с полями:
  - Имя пользователя (обязательное)
  - Email (уникальный, обязательный)
  - Номер телефона (необязательный)
  - Пароль и подтверждение пароля
- Валидация уникальности email
- После успешной регистрации перенаправляет на страницу входа
- Ссылка на страницу входа для существующих пользователей

#### 3. **Вход** (`/login/`)
- Форма входа с использованием стандартной AuthenticationForm
- После успешного входа перенаправляет на страницу профиля
- Ссылка на страницу регистрации для новых пользователей
- Автоматическое перенаправление авторизованных пользователей

#### 4. **Профиль** (`/profile/`)
- Доступна только авторизованным пользователям
- Отображает информацию о пользователе:
  - Имя пользователя
  - Email
  - Номер телефона
  - Дата регистрации
- Использует `LoginRequiredMixin` для защиты доступа
- Красивое оформление в виде информационной карточки

#### 5. **Выход** (`/logout/`)
- Выход из системы
- После выхода перенаправляет на главную страницу
- Отображение сообщения об успешном выходе

## 🎨 Дизайн и стилизация

Проект использует современный адаптивный дизайн с следующими особенностями:

### Цветовая схема
- **Основной градиент**: фиолетово-синий (135deg, #667eea → #764ba2)
- **Фон**: градиентный фон для всего приложения
- **Карточки**: белый фон с тенями для контента

### Особенности дизайна
- ✨ Плавные анимации и переходы
- 📱 Полностью адаптивный дизайн для мобильных устройств
- 🎯 Современная типографика
- 💫 Эффекты при наведении на элементы
- 🔔 Красивое отображение сообщений (success, error, info, warning)

### Статические файлы
Все стили находятся в `main/static/css/style.css` и автоматически подключаются через Django template tags.

## 🔐 Настройки аутентификации

В файле `settings.py` настроены следующие параметры:

```python
AUTH_USER_MODEL = 'main.CustomUser'
LOGIN_URL = 'login'
LOGIN_REDIRECT_URL = 'profile'
LOGOUT_REDIRECT_URL = 'index'
```

## 📄 Основные файлы

### models.py
Определяет модель `CustomUser`, которая наследуется от `AbstractUser` и добавляет поля `email` (уникальное) и `phone_number` (необязательное).

### forms.py
Содержит `CustomUserCreationForm` - кастомную форму регистрации, которая:
- Включает поля email и phone_number
- Валидирует уникальность email
- Использует русские метки для полей
- Настроена для работы с моделью CustomUser

### views.py
Содержит представления:
- `index` - главная страница (функция-представление)
- `ProfileView` - класс-представление для профиля (с `LoginRequiredMixin`)
- `login_view` - страница входа (функция-представление)
- `logout_view` - выход из системы (функция-представление)
- `register_view` - страница регистрации (функция-представление)

### urls.py (main/)
Маршруты приложения:
- `''` → `index`
- `'profile/'` → `ProfileView.as_view()`
- `'login/'` → `login_view`
- `'logout/'` → `logout_view`
- `'register/'` → `register_view`

### static/css/style.css
Основной файл стилей, содержащий:
- Общие стили и сброс CSS
- Стили навигации
- Стили форм и кнопок
- Стили карточек и профиля
- Адаптивные медиа-запросы
- Анимации и переходы

## 🎨 Шаблоны

Все шаблоны наследуются от `base.html`, который содержит:
- Навигационное меню (меняется в зависимости от статуса авторизации)
- Блок для отображения сообщений (messages) с красивым оформлением
- Блок контента для дочерних шаблонов
- Подключение CSS стилей

### Особенности шаблонов:
- **base.html**: Базовый шаблон с навигацией и структурой
- **index.html**: Hero-секция и карточки функций
- **login.html**: Улучшенная форма входа с обработкой ошибок
- **register.html**: Расширенная форма регистрации с валидацией
- **profile.html**: Информационная карточка профиля

## 👤 Админ-панель

Модель `CustomUser` зарегистрирована в админ-панели Django с настройками:
- Отображение полей: username, email, phone_number, is_staff, is_active, date_joined
- Фильтры по статусу и дате регистрации
- Поиск по username, email и phone_number
- Настроенные fieldsets для удобного редактирования

Для доступа к админ-панели:
```
http://127.0.0.1:8000/admin/
```

## 🧪 Тестирование

Для тестирования функциональности:

1. **Регистрация нового пользователя:**
   - Перейдите на `/register/`
   - Заполните форму (email должен быть уникальным)
   - После регистрации вы будете перенаправлены на страницу входа

2. **Вход в систему:**
   - Перейдите на `/login/`
   - Введите учетные данные
   - После входа вы будете перенаправлены на профиль

3. **Просмотр профиля:**
   - После входа автоматически откроется профиль
   - Или перейдите на `/profile/` вручную
   - Проверьте отображение всех данных пользователя

4. **Выход из системы:**
   - Нажмите "Выход" в навигации
   - Вы будете перенаправлены на главную страницу

5. **Проверка защиты страниц:**
   - Попробуйте открыть `/profile/` без авторизации
   - Вы будете автоматически перенаправлены на страницу входа

## 📌 Важные замечания

- При первом запуске после создания кастомной модели пользователя необходимо выполнить миграции
- Если база данных уже содержит пользователей со стандартной моделью, может потребоваться пересоздать базу данных
- В режиме разработки используется SQLite, для продакшена рекомендуется использовать PostgreSQL или MySQL
- Статические файлы автоматически обрабатываются Django в режиме разработки
- Для продакшена необходимо настроить сборку статических файлов через `python manage.py collectstatic`

## 🔧 Разработка

Проект создан в рамках учебного задания по Django и демонстрирует:
- Работу с кастомными моделями пользователей
- Создание кастомных форм регистрации
- Использование миксинов для защиты представлений
- Настройку системы аутентификации Django
- Создание современного адаптивного дизайна
- Работу со статическими файлами в Django

## 📦 Сборка архива проекта

Для создания архива проекта (исключая ненужные файлы):

```bash
cd /Users/sergejsaburkin/PythonProject/DZ_DJ05_Django
zip -r auth_test_hw.zip auth_test_hw -x "*/__pycache__/*" "*.pyc" "*.pyo" "*/.DS_Store"
```

## 📞 Контакты

Для вопросов и предложений создайте issue в репозитории проекта.

---

**Версия:** 2.0  
**Дата обновления:** 2025  
**Лицензия:** MIT

